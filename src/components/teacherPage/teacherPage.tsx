import { TopBar } from "@components/topbar";

export default function TeacherPage() {
    return (
        <div>
            <TopBar title={"Teacher"} />
            <h1>Teacher</h1>
        </div>
    );
}
